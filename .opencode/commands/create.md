---
description: Initialize a new project or boilerplate from standard templates
agent: app-builder
---

Start scaffolding a new project or major module based on the request: $ARGUMENTS

### Create Requirements:
1. **Analyze:** Evaluate the 26 available templates in `.opencode/skills/app-builder/templates/`.
2. **Match:** Select the best matching template for the language/framework requested.
3. **Initialize:** Read the selected `TEMPLATE.md` to extract the correct setup commands (e.g., `npx`, `cargo`, `dotnet new`).
4. **Run:** Execute the setup commands safely.
5. **Next Steps:** Suggest the next immediate actions for the user.
